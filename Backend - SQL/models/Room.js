// import mongoose from "mongoose";

// const roomSchema = new mongoose.Schema({
//   room_name: { type: String, required: true, unique: true }, // ชื่อห้องประชุม
//   capacity: { type: Number, required: true }, // ความจุห้อง
//   location: { type: String, required: true }, // ที่ตั้งห้อง
//   description: { type: String }, // คำอธิบายเพิ่มเติม
//   created_at: { type: Date, default: Date.now }, // วันที่สร้างห้อง
// });

// export default mongoose.model("Room", roomSchema);